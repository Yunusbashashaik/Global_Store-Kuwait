# GoDaddy notes

The public catalog is **not** stored here. Add services in `shared/defaultServices.js` and images in `client/public/service-images/`.

This folder may still receive `latest-settings.json` when Admin saves contact / About Us settings. Do not copy old `latest-catalog.json` files onto the server.
